import 'package:flutter/material.dart';

class StudentTabScreen extends StatelessWidget {
  const StudentTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Student Tab Screen"),
    );
  }
}
